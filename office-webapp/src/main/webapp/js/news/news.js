var _grid = "gridMember";

$(document).ready(function() {
  //grid
  NewsGrid.watable = gridUtils.initGrid(NewsGrid, true);
  
  // $('#table_'+NewsGrid.id).modal("show");
});

/**
 * chrisryo
 */
var News = function() {
	return {

	}
}();